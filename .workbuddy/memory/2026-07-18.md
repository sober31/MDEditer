# 2026-07-18

## MDEditer 升级：Vim 风格 + 单文件独立应用
- 编辑器内核由 `<textarea>` 替换为 **CodeMirror 6 + @replit/codemirror-vim**（真实 Vim 模态编辑：普通/插入/可视、hjkl、dd/yy/p、u/Ctrl-r、/ 搜索、`:w`/`:wq`/`:x` 保存、底部 `-- INSERT --` 状态栏）
- 构建链：在受管 node 工作区 `/Users/gang.lan/.workbuddy/binaries/node/workspace` 安装 `@codemirror/*`、`@replit/codemirror-vim`、`esbuild`；`build/editor.js` 用 esbuild 打包为 IIFE `dist/cm.js`（640KB，离线）
  - esbuild 解析坑：entry 在 MDEDITER/build，node_modules 在受管工作区，需用 `nodePaths` 指定；且不能直接 `node bin/esbuild`（该 bin 是原生二进制），要用 JS API `require('esbuild').build`
- app.js 重构：所有 `editor.value` 读写改为 `window.MDEditer` 实例（getValue/setValue/surround/linePrefix/insertBlock/undo/redo/setTheme/getCursor）；onChange 驱动预览渲染+自动保存；`:w` 经 `Vim.defineEx` 回调 saveFile
- 撤销/重做：Vim 模式下改用 CodeMirror 内置 history（u / Ctrl-r），工具栏按钮调用 ed.undo/redo（此前自管 50 步上限被 Vim 模型取代）
- 单文件打包 `MDEditer.html`（740KB，全内联 styles/marked/purify/cm/app，无外部资源、无联网）：用正则 `<script src="PATH">` / `<link href>` 内联；内联 JS 须把 `</script` 转义为 `<\/script`（已用 regex 全局处理）
- 验证：node --check 通过；jsdom 集成测试（多文件版 + 单文件版）均 PASS——启动、预览渲染、checkbox 点击 `-[x]`↔`-[ ]` 同步源码；本地 http.server:8080 全部 200
- 交付物：`/Users/gang.lan/AI_PROJECTS/MDEDITER/MDEditer.html`（Mac/Windows 双击即用，浏览器打开）；开发版 `/Users/gang.lan/AI_PROJECTS/MDEDITER/{index.html,app.js,styles.css,dist/cm.js}`
- 说明：单 HTML 靠浏览器运行，跨平台“下载即运行”；若要真·原生 .app/.exe 需 Tauri/Electron 分别构建（本机只能出 Mac 版）
